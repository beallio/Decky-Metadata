# Controller type filtering

## Date

2026-08-26

## Objective

Implement the `controller-type-filtering` plan: keep Steam's requested source
filter for normal and unknown controller types, force only the matched-source
query to unfiltered for the verified Legion Go S type `102`, and surface the
connected controller types in the Quick Access Menu Versions section.

## Files changed

- `src/types.ts`
- `src/steam/controllerTypes.ts`
- `src/steam/controllerTypes.test.ts`
- `src/steam/controllerLayouts.ts`
- `src/steam/controllerLayouts.test.ts`
- `src/steam.ts`
- `src/ContentPanel.tsx`
- `src/ContentPanel.updateSettings.test.tsx`
- `src/components/qam/VersionsSection.tsx`
- `src/components/qam/VersionsSection.test.tsx`
- `scripts/deck/js/check_controller_layouts.js`
- `tests/test_deck_fixture_selection.py`
- `README.md`
- `CHANGELOG.md`
- `dist/index.js` and generated `dist/index.js.map`

The source map is the Rollup-generated companion to the committed bundle; it
was regenerated by the required build rather than edited manually.

## Controller policy

- The controller is resolved solely by the `controllerIndex` from
  `QueryControllerConfigsForApp` arguments.
- Type `4` (Steam Deck), unknown, unavailable, and external/future types keep
  Steam's requested source filter.
- Type `102` (Legion Go S) is the sole entry in the explicit affected allowlist
  and changes only its supplemental matched-source query to `false`.
- Primary shortcut queries and Steam's visible
  `m_bFilterOtherControllerTypes` setting remain untouched. Steam's existing
  getter equivalence filter therefore continues to control visible results.
- The Versions panel samples `ControllerStore.GetControllers()` once at mount,
  prefers `ControllerStore` over `controllerStore`, and formats sorted unique
  types as diagnostics. It does not enumerate MobX stores or poll hot-plug
  changes.

## Evidence and validation

### Routing and dependency baseline

- `scripts/decky doctor`: repository and tooling checks passed; the initial
  local package check was stale/empty.
- `scripts/decky verify-change dev --explain` initially stopped at the missing
  local TypeScript compiler. Its captured failure is
  `/tmp/Decky-Metadata/controller-type-filtering/verify-change-initial-error.log`.
- `./run.sh npm ci` restored the declared frontend dependency set using the
  project cache policy.

### Device baseline

- Legion Go S baseline:
  `/tmp/Decky-Metadata/controller-type-filtering/legos-before.json`
  - controller index `0`, type `102`, visible filter `true`;
  - Deadpool displayed and matched-source Community counts were both `0`;
  - the separately bounded scalar capture is
    `/tmp/Decky-Metadata/controller-type-filtering/legos-controller-before.json`.
- Steam Deck baseline is blocked: `steamdeck` resolves through SSH config to
  `10.168.168.20`, and the tunnel returned `No route to host`. The captured
  failure is
  `/tmp/Decky-Metadata/controller-type-filtering/steamdeck-tunnel-error.log`.
  No alternate host, fixture, or inferred controller was used.

### Red/green tests and mutation controls

- The resumed worktree's first focused run recorded `18 passed | 58 failed`
  in `/tmp/Decky-Metadata/controller-type-filtering/focused-red.log`. It exposed
  an overridden injected target dependency, invalid component render-harness
  assumptions, and a numeric sort expectation inconsistent with the plan.
- A follow-up focused run isolated the final effective-key expectation mismatch
  (`75 passed | 1 failed`) in
  `/tmp/Decky-Metadata/controller-type-filtering/focused-second-red.log`.
  The plan requires changing controller index to refetch, so the assertion now
  expects that source query.
- Restored focused command:
  `./run.sh npx vitest run src/steam/controllerTypes.test.ts src/steam/controllerLayouts.test.ts src/components/qam/VersionsSection.test.tsx src/ContentPanel.updateSettings.test.tsx`
  - `4` files, `76 passed`.
- Mutation control: removing the type `102` allowlist entry produced `4` named
  failures (`forces false only for affected types`, the affected source filter,
  exact-index, and effective-key layout tests) in
  `/tmp/Decky-Metadata/controller-type-filtering/mutation-affected-type-red.log`.
  The change was restored without committing it.
- Mutation control: omitting the Versions line produced `4` exact-text failures
  in
  `/tmp/Decky-Metadata/controller-type-filtering/mutation-versions-red.log`.
  The line was restored without committing it.
- Full restored project validation:
  - TypeScript type-check passed;
  - Rollup build regenerated the committed artifact;
  - frontend Vitest: `19` files, `219 passed`;
  - Python byte-compilation passed;
  - pytest: `400 passed`;
  - `scripts/orchestration/run-quality-gates`: `quality-gates: OK`;
  - `scripts/orchestration/check-review-notes-not-deleted`: passed.

## Hardware validation status

This round is not complete yet. The Steam Deck pre-deploy baseline, both
post-deploy layout probes, the bounded Deck verification suite, actual Versions
panel DOM assertions/screenshots, and controller focus checks remain unverified.
They require `steamdeck` to become reachable again; no game was launched, and
no layout was selected, previewed, or applied. The Legion Go S deployment and
after-probe also remain pending so the prescribed two-device verification order
can be completed.

## Unverified by plan design

- Layout preview, selection, application, and game launch.
- Hardware behavior for controller types other than `4` and `102`.
- Physical multiple-controller presentation.
- Live hot-plug updates while the panel remains mounted.
