# Production release v0.3.13

Date: 2026-09-07

## Authorization and publication

The maintainer approved dev-to-main promotion and explicitly selected public
GitHub publication. Released `v0.3.13` from main commit
`0588beb4810c8b0046bf9f023278cb24e29cf3b6`.

Title: **v0.3.13 — Add Steam shortcut names and fix stale Activity news**

Release: https://github.com/beallio/Decky-Metadata/releases/tag/v0.3.13

The notes describe manual Steam shortcut-name preview/confirmation, original-name
restoration, controller access to the section, Activity cache removal, and title
matching. A cropped real Deck screenshot is committed at
`assets/decky-metadata-shortcut-name.png`, embedded in the release notes through
the immutable v0.3.13 tag, and linked from README with the 20260907 cache buster.

## Gate correction

The new README picture exposed a test that required exactly seven images.
Removed that fixed-count assertion instead of changing it to eight. Existing
checks still require referenced image files to exist, be tracked, and use cache
busters. The focused release-asset checks and full quality gate then passed.

## Verification

- Local gate passed 27 frontend files / 432 tests, TypeScript, Rollup, Python
  compilation, pytest, and review-note integrity.
- GitHub CI run 34148963421 and Release run 34148965354 succeeded.
- Public release is neither draft nor prerelease and is GitHub's latest stable.
- Downloaded the published ZIP, manifest, and checksum. All agree on SHA-256:
  `d173c50440e08eb670589bbc2d818fced60c09e7bb6ed1748e6fc05fe0147633`.
- Both metadata files inside the ZIP say 0.3.13 without a build suffix. Plugin
  display identity remains `Decky Metadata`; archive root remains Decky-Metadata.
- Verified the public notes contain the feature image and issue #3 link, and the
  tagged image blob matches the local committed picture.
- Issue #3 was closed as completed, given the `fixed` label, and linked to this
  release with the on-device removal verification result.
- Game-launch checks remain excluded by the maintainer's earlier instruction.

After publication, prepared the next local development base at 0.3.14 through
`scripts/bump_next_patch.sh`. Main and v0.3.13 were pushed; dev remains local-only.
